# Export Canvas Course File / Page Structure
This script takes a canvas instance url, user token, and course id and generates a list of JSON files. Can be used to make a search tool for very large courses.
